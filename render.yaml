services:
  - type: web
    name: jobright-ai-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python -m spacy download en_core_web_sm && alembic upgrade head
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: SECRET_KEY
        value: jobright-ai-super-secure-secret-key-2024-production
      - key: ENVIRONMENT
        value: production
      - key: DEBUG
        value: false
      - key: SCRAPING_ENABLED
        value: false
      - key: JWT_SECRET_KEY
        value: jobright-ai-jwt-super-secure-key-2024
      - key: DATABASE_URL
        fromDatabase:
          name: jobright-db
          property: connectionString
      - key: REDIS_URL
        value: memory://

databases:
  - name: jobright-db
    databaseName: jobright_production
    user: jobright_user
    plan: free